# Secrets — referenced from device configs via !secret
# Replace placeholders before compiling/flashing.

wifi_ssid: "SSID"
wifi_password: "PASSWORD"
ap_password: "AP-PASSWORD"
ota_password: "OTA-PASSWORD"
